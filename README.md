# Mark repo

Download and store mark images 

## Usage

You could modify the constant `BATCH_DOWNLOAD_SIZE` to control the amount of images that  download parallelly

```bash
pnpm install
pnpm run build
sh deploy.sh
```